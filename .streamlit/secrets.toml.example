# Copy this file to `secrets.toml` (same folder) and paste your real FRED API key.
# `secrets.toml` is gitignored, so your key never gets committed.
# Get a free key at: https://fredaccount.stlouisfed.org/apikeys

FRED_API_KEY = "your_32_character_fred_api_key_here"
